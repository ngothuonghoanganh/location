import * as React from 'react';
import * as d3Shape from 'd3-shape';
import { CircleProps } from 'react-roughjs';
import { BaseChartComponentProps, Curve } from '../baseTypes';
export interface LineSeriesProps<T extends object> extends BaseChartComponentProps {
    dataKey: keyof T;
    curve?: Curve;
    children?: (item: T, props: CircleProps, index: number) => React.ReactNode;
}
export declare const LineSeries: {
    <T extends object>(props: LineSeriesProps<T>): JSX.Element;
    displayName: string;
    defaultProps: {
        curve: d3Shape.CurveCardinalFactory;
    };
};
export default LineSeries;
