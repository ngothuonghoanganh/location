import * as React from 'react';
import { BaseOptions } from '../baseTypes';
export interface PathProps extends BaseOptions {
    d?: string;
}
export declare const Path: React.FC<PathProps>;
export default Path;
