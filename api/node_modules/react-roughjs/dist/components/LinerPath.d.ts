import * as React from 'react';
import { BaseOptions, Point } from '../baseTypes';
export interface LinearPathProps extends BaseOptions {
    points?: Point[];
}
export declare const LinearPath: React.FC<LinearPathProps>;
export default LinearPath;
