import * as React from 'react';
import { BaseOptions, Point } from '../baseTypes';
export interface CurveProps extends BaseOptions {
    points?: Point[];
}
export declare const Curve: React.FC<CurveProps>;
export default Curve;
